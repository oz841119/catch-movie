import { createStore } from 'vuex'
import api from './api'
import movieBox from './movieBox'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    api,
    movieBox
  }
})
